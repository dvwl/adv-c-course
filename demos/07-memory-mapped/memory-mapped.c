// Ensure that you installed the SDK
// or have the right libraries in place
#include <stdio.h>
#include <windows.h>

// On a Windows machine, if Line 4 shows
// a red squiggly line means you don't
// have the SDK installed correctly.
// Check slides.
